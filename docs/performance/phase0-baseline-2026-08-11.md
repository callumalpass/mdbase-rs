# Phase 0 baseline observations — 2026-08-11

These are release-mode, local mdbase-rs observations for the coordinated local
collection runtime task. They are informational: no latency or RSS threshold is
enforced. The complete machine-readable report is generated by
`scripts/phase0-baseline.sh` at
`target/benchmarks/phase0-baseline-current/phase0-baseline.json` (the checked-in JSON
summary is `benchmarks/phase0-baseline-2026-08-11.json`).

Source pins for this run:

- mdbase-rs: `a36341947b9dfd1c01d68030a73f542528d16864`
- mdbase-connect reference baseline: `d4138fb93ea0606d141139c96062136fee1cd409`
- host: Linux x86_64, 16 logical CPUs

## Workload summary

| Synthetic records | Setup ms | 200-row query p95 ms | Sequential pagination | Cache rebuild mean ms | Update mean ms | Rename+refs mean ms |
|---:|---:|---:|---:|---:|---:|---:|
| 2,000 (+100 refs) | 71.13 | 211.66 | 2,100 rows / 11 pages / 117.18 ms p95 | 128.13 | 59.13 | 1,727.67 |
| 10,000 (+100 refs) | 296.31 | 554.35 | 10,100 rows / 51 pages / 2,289.95 ms p95 | 274.59 | 155.17 | 2,388.06 |

Create/delete means were 32.69/16.32 ms at 2,000 records and 33.73/17.81 ms at
10,000 records. Provider mutation versus explicit watcher synchronization was
150.52/88.78 ms and 4,125.60/770.80 ms respectively. Query phase data retains
cache-refresh timing: mean 24.37 ms (2,000) and 197.89 ms (10,000). These
observations deliberately preserve the high-cost paths that the coordinated runtime
must remove; individual timings are not release thresholds.

## Concurrency and memory observations

| Records | Workload | Threads | Requests | Elapsed ms | Requests/s | Errors |
|---:|---|---:|---:|---:|---:|---:|
| 2,000 | mixed provider work, one collection | 4 | 160 | 28,916.10 | 5.53 | 0 |
| 2,000 | two active collections | 4 | 160 | 405.81 | 394.28 | 0 |
| 10,000 | mixed provider work, one collection | 4 | 160 | 63,181.91 | 2.53 | 0 |
| 10,000 | two active collections | 4 | 160 | 1,035.10 | 154.57 | 0 |

The 1,600-request read/query soak completed with zero errors. Between-request
RSS/PSS samples were 28,152/25,511 KiB at baseline and sampled peak for 2,000 records, ending at
28,152/25,511 KiB. For 10,000 records the baseline was 76,652/74,011 KiB; the
sampled peak and final sample were 76,652/74,012 KiB. These samples do not capture transient
allocations inside an operation; `/proc` values are observations only, not acceptance
budgets.

## Correctness gates

- 200-row query returned 200 rows at both sizes.
- Sequential pagination returned every row exactly once in the observed page
  sequence (2,100/11 and 10,100/51).
- Create, update, delete, and reference-updating rename succeeded.
- Reference rename checks found rewritten references and no stale target paths.
- Explicit watcher synchronization observed five events per workload.
- All concurrent and RSS requests reported zero operation errors.

## Gaps

- This is a local provider/runtime observation; it does not exercise Connect
  relay, admission/fairness, daemon RSS, or NATS.
- The existing saved-view profile is not duplicated; query phase/cache-refresh
  timings are retained. Run the existing release profiler separately for view
  observations.
- RSS/PSS support is Linux `/proc`-dependent, sampled between requests, and remains non-gating.
